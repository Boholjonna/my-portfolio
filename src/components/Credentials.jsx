import React from 'react';
import GradientText from './GradientText';
import '../styles/Project.css';

const Credentials = () => {
  return (
    <section className="project-container" style={{ position: 'relative', overflow: 'hidden' }}>
      <h1 className="project-title" style={{ position: 'relative', zIndex: 1, marginBottom: '2rem', marginTop: 0, textAlign: 'center' }}>Credentials</h1>
      {/* Add your credentials content here */}
    </section>
  );
};

export default Credentials; 